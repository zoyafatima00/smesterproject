import 'package:flutter/material.dart';

class AltProfile extends StatelessWidget {
  const AltProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
